## Packages
framer-motion | Used for beautiful page transitions and micro-interactions
date-fns | Used for formatting timestamps and dates nicely
lucide-react | Required for dashboard and landing page icons
@hookform/resolvers | Form validation integration
react-hook-form | Form state management

## Notes
- Tailwind configuration expects `--font-sans` to be mapped to Poppins.
- Ensure the backend properly supports the defined `/api/login`, `/api/checkin`, `/api/stats`, and `/api/queue/:department` endpoints as per the manifest.
